server {
    listen 443 ssl;
    server_name tgm.bet;

    ssl_certificate /etc/letsencrypt/live/tgm.bet/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/tgm.bet/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Reverse proxy for the second application using HTTPS on port 7078
server {
    listen 7078 ssl;
    server_name tgm.bet;

    ssl_certificate /etc/letsencrypt/live/tgm.bet/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/tgm.bet/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://localhost:8071;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

"tgm.conf" 51L, 1545B 51,0-1 Bot